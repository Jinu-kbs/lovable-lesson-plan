# 헤더·사이트맵 정보구조 재설계 제안

**작성일**: 2026-09-04
**작성자**: 김병선 (KBS Lab)
**대상**: 김선생의 바이브코딩 가이드 (https://jinu-kbs.github.io/lovable-lesson-plan/)
**범위**: 전역 헤더, 교안 페이지 헤더, 사이트맵, 분류 체계
**버전**: v1.0
**성격**: 진단 + 제안. 코드 변경은 승인 후 별도 수행

---

## 목차

1. [한 줄 판정](#1-한-줄-판정)
2. [진단 — 측정값 8건](#2-진단--측정값-8건)
3. [제안 — 우선순위 7건](#3-제안--우선순위-7건)
4. [제안 1: 헤더 3줄을 2줄로](#4-제안-1-헤더-3줄을-2줄로)
5. [제안 2: 전체 교안 메가패널](#5-제안-2-전체-교안-메가패널)
6. [제안 3: 학습 경로를 1급 시민으로](#6-제안-3-학습-경로를-1급-시민으로)
7. [제안 4: 사이트맵 신설](#7-제안-4-사이트맵-신설)
8. [제안 5: 분류 재편](#8-제안-5-분류-재편)
9. [단계별 실행안](#9-단계별-실행안)
10. [핵심 가정과 검증 체크리스트](#10-핵심-가정과-검증-체크리스트)

---

## 1. 한 줄 판정

**헤더가 정보를 못 주는 게 아니라, 같은 정보를 세 번 주고 정작 필요한 것을 안 준다.**

교안 페이지 헤더는 현재 도구와 그 3레벨을 세 줄에 걸쳐 반복하면서, 45개 교안 중
다른 도구로 가는 길과 검색은 하나도 제공하지 않는다. 사이트가 커지는 동안 헤더만
초기 3개 교안 시절 구조에 머물러 있다.

---

## 2. 진단 — 측정값 8건

전부 로컬 서버에서 실측했다. 추정치가 아니다.

### 2.1 교안 헤더가 같은 말을 세 번 한다

1280×800 데스크톱 기준, 본문이 시작되기 전 크롬이 **431px — 첫 화면의 54%**를 쓴다.

| 순서 | 요소 | 높이 | 내용 |
|------|------|------|------|
| 1 | `topnav` | 51px | 바이브코딩 가이드 / MCP 초급 · MCP 중급 · MCP 개발자 |
| 2 | `breadcrumb` | 41px | 홈 > MCP > 중급자편 |
| 3 | `lesson-topnav-bar` | 44px | MCP / 초보자 · 중급자 · 개발자 |
| 4 | `hero` | 295px | 제목 · 부제 · 배지 4개 · 소요시간 |
|   | **합계** | **431px** | 본문 시작 y=535 |

**1행과 3행은 같은 링크 3개다.** 하나는 "MCP 초급"이라 쓰고 하나는 "초보자"라 쓸 뿐,
목적지가 동일하다. 2행 브레드크럼도 같은 도구명을 세 번째로 반복한다.

### 2.2 교안에서 다른 도구로 갈 수 없다

교안 페이지 `topnav`의 링크는 총 4개다. 홈 1개 + 현재 도구의 3레벨.
**도구가 15종인데 헤더는 1종만 보여준다.** 러버블 교안에서 커서 교안으로 가려면
반드시 홈을 경유해야 한다. 45개 교안 전부가 같은 구조다.

### 2.3 검색이 홈에만 있다

- `search.html`은 상단 메뉴 어디에도 링크되어 있지 않다.
- 유일한 입구는 홈 히어로의 검색창 하나.
- 교안 페이지의 검색 입력 요소: **0개**.

전문 검색 인덱스(`search-content.json`)를 만들어 두고도 진입로가 한 곳뿐이다.

### 2.4 모바일 교안 헤더가 깨진다

375px 뷰포트에서 교안 `topnav`의 링크 4개가 **2줄로 접히고 단어 중간에서 잘린다.**

```
바이브코딩 가이     MCP 초    MCP 중    MCP 개발
드                  급        급        자
```

- 링크 개별 높이 52px (2줄), 헤더 전체 77px
- **교안 페이지에는 햄버거 버튼이 없다.** 홈에만 있다.
- 결과적으로 모바일 교안 사용자는 접힌 4개 링크가 내비게이션의 전부다.

### 2.5 사이트맵이 존재하지 않는다

- `sitemap.html` 없음, `sitemap.xml` 없음, `robots.txt` 없음
- 푸터 링크 총 3개: 홈, Threads, Instagram
- **61개 HTML 페이지의 전체 지도를 볼 수 있는 화면이 사이트에 없다.**

검색엔진 관점에서도 45개 교안이 홈의 카드 그리드 한 곳에서만 링크된다.

### 2.6 데이터에 학습 경로 5개가 있는데 어디에도 안 보인다

`lessons-data.js`가 이미 경로를 정의하고 있다.

| 경로 | 구성 |
|------|------|
| 입문자 경로 | 웹개발기초 → AI Studio → 러버블 초급 → 러버블 중급 |
| 기본기 경로 | 개발인터페이스 → Git/GitHub → 배포 → 웹개발 중급 |
| 중급자 경로 | 프롬프트 → ChatGPT → 구글 → 커서 |
| Claude 생태계 경로 | Claude Code → MCP → Cowork → Design |
| 개발자 경로 | 커서 개발자 → Claude Code 개발자 → 안티그래비티 개발자 → 배포 개발자 |

이것이 "어디서 시작하지"에 대한 사이트 자신의 답인데, **교안 안쪽 작은 배너로만 나온다.**
헤더에도 홈 첫 화면에도 없다. 홈 히어로의 버튼 3개(처음 시작하기·전체 교안 보기·심화 학습)는
경로가 아니라 같은 페이지 내 스크롤 이동이다.

### 2.7 분류 이름이 내용과 어긋난다

현재 상단 메뉴는 4개 드롭다운에 링크 56개를 담고 있다.

| 드롭다운 | 도구 수 | 하위 링크 | 어긋남 |
|----------|---------|-----------|--------|
| AI 빌더 | 4 | 12 | 커서는 빌더가 아니라 IDE다 |
| Claude 생태계 | 5 | 13 | MCP는 벤더 중립 공개 프로토콜인데 여기 있다 |
| 기초·인프라 | 7 | 21 | ChatGPT/Codex, 프롬프트 엔지니어링은 인프라가 아니다 |
| 심화·리포트 | - | 7 | - |

「기초·인프라」 하나가 7개 도구 21개 링크를 지고 있어 다른 셋과 무게가 맞지 않는다.
사용자가 특정 교안을 찾을 때 **어느 드롭다운에 있는지 외워야 한다.**

### 2.8 드롭다운이 hover 전용이다

`.nav-dropdown:hover .dropdown-content{display:block}` 한 줄이 유일한 열림 조건이다.

- 키보드 Tab으로 접근해도 열리지 않는다
- 터치 장치(태블릿)에서 열 수단이 없다
- `aria-expanded` 등 상태 표시 없음

---

## 3. 제안 — 우선순위 7건

| 순위 | 제안 | 해결하는 진단 | 난이도 | 효과 |
|------|------|---------------|--------|------|
| P1 | 교안 헤더 3줄 → 2줄 통합 | 2.1 | 낮음 | 첫 화면 45px 회복, 중복 제거 |
| P2 | 「전체 교안」 단일 메가패널 | 2.2, 2.7 | 중간 | 어느 페이지에서든 45개 교안 도달 |
| P3 | 검색을 전 페이지 헤더 상수로 | 2.3 | 낮음 | 이미 만든 검색 인덱스 활용 |
| P4 | 사이트맵 페이지 + 푸터 링크맵 | 2.5 | 낮음 | 전체 조망, 검색엔진 수집 |
| P5 | 분류를 목적 기반 5군으로 재편 | 2.7 | 중간 | 외우지 않아도 찾음 |
| P6 | 모바일 교안 헤더 수리 | 2.4 | 낮음 | 깨짐 해소 |
| P7 | 드롭다운 클릭·키보드 접근 | 2.8 | 낮음 | 접근성 |

**공통 원칙**: 헤더 마크업을 페이지마다 손으로 쓰지 않는다. `lessons-data.js`를 단일 원본으로 두고
`common.js`가 생성한다. 지금 홈의 상단 메뉴 56개 링크는 손으로 쓴 것이라, 교안 하나를 추가할 때마다
사람이 잊지 않고 고쳐야 한다.

---

## 4. 제안 1: 헤더 3줄을 2줄로

### 현재

```
[바이브코딩 가이드]                    MCP 초급  MCP 중급  MCP 개발자   ← 51px
홈 > MCP > 중급자편                                                    ← 41px
MCP  |  초보자  [중급자]  개발자                                       ← 44px
```

### 제안

```
[바이브코딩 가이드]  전체 교안 ▾  학습 경로 ▾  읽을거리 ▾   [검색]  [온라인 랩]   ← 51px (전역)
홈 > MCP > 중급자편              ·        초보자  [중급자]  개발자              ← 40px (맥락)
```

**1행은 모든 페이지가 동일하다.** 홈이든 교안이든 같은 것이 같은 자리에 있다.
**2행은 교안에서만 나온다.** 왼쪽은 현재 위치(브레드크럼), 오른쪽은 레벨 전환.
지금 3행에 있던 레벨 전환을 2행 오른쪽으로 흡수해 한 줄을 없앤다.

- 높이: 136px → **91px** (45px 절감, 첫 화면 5.6% 회복)
- 중복: 도구명 3회 노출 → 1회
- 1행은 `common.js`가 `lessons-data.js`로 생성하므로 교안 추가 시 사람이 손댈 곳이 없다

---

## 5. 제안 2: 전체 교안 메가패널

4개의 hover 드롭다운을 **하나의 「전체 교안」 패널**로 합친다.

```
┌── 전체 교안 ─────────────────────────────────────────────────┐
│  만들기            AI 다루기          개발 기초       연결·배포 │
│  러버블  초·중·개   프롬프트 초·중·개  웹앱기초 초·중·개  MCP 초·중·개│
│  AI Studio 초·중·개 ChatGPT 초·중·개   개발환경 초·중·개  배포 초·중·개│
│  안티그래비티 초·중·개 Cowork 초·중·개  Git/GitHub 초·중·개 구글 초·중·개│
│  커서    초·중·개   Design 초·중·개                             │
│  Claude Code 초·중·개 클로드스킬                                │
├──────────────────────────────────────────────────────────────┤
│  읽을거리  트렌드 리포트 · CC 로드맵 · NVIDIA GTC · 하네스 · SE · 도구비교 │
└──────────────────────────────────────────────────────────────┘
```

**왜 하나로 합치나**: 지금은 "커서 중급자편"을 찾으려면 AI 빌더인지 기초·인프라인지
먼저 맞춰야 한다. 한 패널에 전부 펼치면 맞출 필요가 없다. 도구가 15종이면 4열 그리드
한 화면에 들어간다.

**중요**: 이 패널은 홈과 교안 페이지가 **같은 마크업**을 쓴다. 현재는 홈에만 있고
교안에는 없어서 진단 2.2가 생겼다.

---

## 6. 제안 3: 학습 경로를 1급 시민으로

`lessons-data.js`에 이미 있는 5개 경로를 두 곳에 노출한다.

1. **헤더 「학습 경로 ▾」** — 5개 경로를 각 4단계 순서와 함께 표시
2. **홈 첫 화면** — 현재 히어로 버튼 3개(페이지 내 스크롤)를 경로 5개 카드로 교체

교사가 처음 사이트에 왔을 때 묻는 질문은 "어떤 도구가 있나"가 아니라
**"나는 어디서 시작하나"**다. 사이트는 이미 그 답을 데이터로 갖고 있으면서
화면에 내놓지 않고 있다.

---

## 7. 제안 4: 사이트맵 신설

### 7.1 `sitemap.html` (사람용)

61개 페이지 전체를 한 화면에. 5개 군 × 도구 × 3레벨 표 + 읽을거리 + 특별 페이지.
푸터에서 항상 링크한다.

### 7.2 `sitemap.xml` (검색엔진용)

45개 교안이 현재 홈 카드 그리드 한 곳에서만 링크된다. 사이트맵 XML과
`robots.txt`를 두면 수집 경로가 확실해진다. GitHub Pages는 정적 파일을 그대로
서빙하므로 파일 두 개면 끝난다.

### 7.3 푸터 링크맵

현재 푸터 링크 3개를 4열 링크맵으로 확장한다. 페이지 끝까지 읽은 사람에게
다음 갈 곳을 주는 자리인데 지금은 비어 있다.

---

## 8. 제안 5: 분류 재편

현재 분류는 **도구의 출신**(누가 만들었나)으로 나뉘어 있다.
제안은 **학습자의 목적**(무엇을 하려는가)으로 나눈다.

| 새 분류 | 담기는 것 | 나누는 기준 |
|---------|-----------|-------------|
| **만들기** | 러버블 · AI Studio · 안티그래비티 · 커서 · Claude Code | 결과물이 동작하는 앱 |
| **AI 다루기** | 프롬프트 엔지니어링 · ChatGPT/Codex · Claude Cowork · Claude Design · 클로드 스킬 | 결과물이 문서·디자인·작업 |
| **개발 기초** | 웹·앱 개발 기초 · 개발 인터페이스/터미널 · Git/GitHub | 도구와 무관한 기반 지식 |
| **연결·배포** | MCP · 배포 · 구글 생태계 | 만든 것을 바깥과 잇는 일 |
| **읽을거리** | 트렌드 리포트 · CC 로드맵 · NVIDIA GTC · 하네스 · SE · 도구 비교 | 교안이 아님 |

### 현재 대비 이동

| 항목 | 현재 | 제안 | 이유 |
|------|------|------|------|
| 커서 | AI 빌더 | 만들기 | 유지 (앱을 만든다) |
| ChatGPT/Codex | 기초·인프라 | AI 다루기 | 인프라가 아니다 |
| 프롬프트 엔지니어링 | 기초·인프라 | AI 다루기 | 인프라가 아니다 |
| 구글 생태계 | 기초·인프라 | 연결·배포 | 외부 서비스 연동 |
| MCP | Claude 생태계 | 연결·배포 | 벤더 중립 공개 프로토콜 |
| Claude Cowork/Design | Claude 생태계 | AI 다루기 | 앱이 아니라 문서·디자인 산출 |

무게 분포: 현재 4/5/7/6 → 제안 **5/5/3/3/6**. 가장 무거운 군이 7에서 6으로 내려간다.

**주의**: 이 재편은 CLAUDE.md의 색상 표와 `lessons-data.js`의 구조에 영향을 준다.
색상은 도구별로 고정되어 있으므로 분류가 바뀌어도 색은 그대로 간다.

---

## 9. 단계별 실행안

| 단계 | 내용 | 파일 | 예상 |
|------|------|------|------|
| 1단계 | 헤더 2줄 통합 + 모바일 수리 (P1·P6) | `common.js`, `common.css` | 교안 45개 일괄 적용 |
| 2단계 | 전체 교안 메가패널 생성기 (P2) | `common.js`, `lessons-data.js` | 홈 손코딩 56링크 제거 |
| 3단계 | 헤더 검색 + 드롭다운 접근성 (P3·P7) | `common.js`, `search.js` | |
| 4단계 | 사이트맵 3종 (P4) | `sitemap.html`, `sitemap.xml`, 푸터 | |
| 5단계 | 분류 재편 (P5) | `lessons-data.js`, `CLAUDE.md`, `PRD.md` | 문서 동기화 필수 |

1~3단계는 서로 독립적이지 않다. 전부 `common.js`의 헤더 생성부를 건드리므로
**한 번에 설계하고 순차 적용**하는 편이 안전하다.

---

## 10. 핵심 가정과 검증 체크리스트

### 핵심 가정 3줄

1. **주 사용자는 교사이고, 도구 이름보다 "무엇을 하려는가"로 찾는다** — 분류 재편(P5)의 전제다.
   실제 사용자가 도구명을 알고 오는 비율이 높다면 재편의 이득이 줄어든다.
2. **교안 간 이동 수요가 실재한다** — P2의 전제다. 한 도구만 보고 이탈하는 패턴이라면
   메가패널의 효과가 작다. 현재 사이트에 분석 도구가 없어 측정된 바 없다.
3. **헤더 마크업을 `common.js` 생성으로 옮겨도 SEO·초기 렌더에 손해가 없다** —
   GitHub Pages 정적 사이트에서 JS 생성 내비게이션은 크롤러가 읽지만,
   사이트맵(P4)을 함께 두는 것이 그 보험이다.

### 검증 체크리스트 (PI 김병선 직접 확인)

- [ ] 분류 5군의 이름과 소속이 교사 눈높이에 맞는가 — 특히 "AI 다루기"가 통하는 말인가
- [ ] MCP를 「Claude 생태계」에서 「연결·배포」로 옮기는 것에 동의하는가
- [ ] 홈 히어로 버튼 3개를 학습 경로 5개로 바꿔도 되는가 (현재 버튼은 스크롤 이동이라 기능이 다름)
- [ ] 헤더에서 「AI 교육철학」·「AI 과학교육」 상시 노출을 유지할지 (현재 1행 고정)
- [ ] 「온라인 랩」 외부 링크의 헤더 내 위치
- [ ] 사이트맵 페이지를 공개 메뉴에 넣을지, 푸터에만 둘지
- [ ] 실행 단계 1~5 중 어디까지 이번에 진행할지

### 이번 진단 중 함께 처리한 것

색상 작업(직전 커밋 `677cfe5`)에서 **16진수만 치환하고 rgba 표기를 놓쳐**
헤더·히어로에 구 보라색 25건이 남아 있었다. 이번에 정리했다.

- `index.html` 23건, `harness-engineering.html` 2건
- 대상: 상단 메뉴 테두리·호버 배경, 드롭다운 테두리, 히어로 글로우, 아바타 링, 햄버거 호버
- 교안 본문 안의 색(예: `chatgpt-beginner.html`의 "메인 색상: #5052EC")은
  **학습 내용이므로 바꾸지 않았다**

---

**작성**: Claude Code · 2026-09-04
**다음 행동**: 위 체크리스트 회신 후 1단계 착수
